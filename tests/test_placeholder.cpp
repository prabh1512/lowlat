// Placeholder test — confirms the test harness builds and runs.
// Real tests will be added per-component as we build them.

#include <gtest/gtest.h>

TEST(Placeholder, BuildSystemWorks) {
    EXPECT_EQ(1 + 1, 2);
}
